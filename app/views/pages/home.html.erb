<div>

  <h1> BORDEAUX VISUM </h1>
  <h2> Partagez vos moments forts à Bordeaux en 3D </h2>
</div>

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url(<%=asset_path 'lion-bleu-place-stalingrad.jpg'%>);">
      </div>

      <div class="tabs">
        <a class="tab active" href="#">
          <h3>Thèmes</h3>
        </a>
        <a class="tab" href="#">
          <h3>Lieux</h3>
        </a>
      </div>

<div class="poi">
  <%= link_to "Trouver un point d'intérêt", pois_path %>
</div>

<div class="Créer un point d'intérêt">
  <%= link_to "Créer une photo", new_poi_path %>
  <img tag= "diaphragme-appareil-photo.jpg">
</div>

<img class="card-user avatar" src="user.jpg">
  <a class="card-link" href="#" ></a>
  <img tag= "" >
</div>

<div class="map">
  <div class="col-sm-8">

  </div>
  <div class="col-sm-4">
     <div id="map" style="width: 100%; height: 300px;"></div>


      <% content_for(:after_js) do %>
        <%= javascript_tag do %>
          $(document).ready(function() {
            var handler = Gmaps.build('Google');
            handler.buildMap({ internal: { id: 'map' } }, function() {
              markers = handler.addMarkers(<%= raw @hash.to_json %>);
              handler.bounds.extendWith(markers);
              handler.fitMapToBounds();
              if (markers.length == 0) {
                handler.getMap().setZoom(2);

              } else if (markers.length == 1) {

                handler.getMap().setZoom(14);
              }
            });
          });
        <% end %>
      <% end %>
   </div>
</div>

